# ── Database (Neon PostgreSQL) ─────────────────────────────────────────────
DATABASE_URL=postgresql+psycopg2://username:password@ep-xxxx-pooler.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL_DIRECT=postgresql+psycopg2://username:password@ep-xxxx.region.aws.neon.tech/dbname?sslmode=require

# ── Redis ──────────────────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379

# KYC face-match decision thresholds (approve >= approve; flag >= flag)
KYC_MATCH_APPROVE_THRESHOLD=0.80
KYC_MATCH_FLAG_THRESHOLD=0.60

# ── Auth (Member 7) ────────────────────────────────────────────────────────
JWT_SECRET=your_jwt_secret_here
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=15
JWT_REFRESH_EXPIRE_DAYS=7
OTP_EXPIRE_MINUTES=5

# ── App ────────────────────────────────────────────────────────────────────
APP_ENV=development

# ── ML ─────────────────────────────────────────────────────────────────────
DEEPFACE_MODEL=ArcFace
GROQ_API_KEY=your_groq_api_key_here
CHATBOT_SESSION_RETENTION_DAYS=30

# ── Storage / S3 (Member 3 — KYC docs) ────────────────────────────────────
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_BUCKET_NAME=neobank-kyc-docs
AWS_REGION=eu-central-1
S3_BUCKET=neobank-kyc-docs
S3_REGION=eu-central-1
S3_ENDPOINT_URL=

# -- FCM Web Push (Member 5 - DEVATTECH-109) -------------------------------
FCM_PROJECT_ID=your_firebase_project_id
FCM_CLIENT_EMAIL=your_firebase_service_account_email
FCM_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour_private_key_here\n-----END PRIVATE KEY-----\n"
FCM_TIMEOUT_SECONDS=5

# ── Email (Member 5) ───────────────────────────────────────────────────────
EMAIL_PROVIDER=resend
EMAIL_FROM="NeoBank Lebanon <onboarding@resend.dev>"

SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true

SENDGRID_API_KEY=
RESEND_API_KEY=

# ── Frontend ───────────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8000

# Twilio (SMS OTP delivery)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM_NUMBER=+15555555555
HIGH_VALUE_SMS_THRESHOLD=500
# -- Payment Gateway (Member 4 - top-up) ------------------------------------
PAYMENT_GATEWAY_URL=http://gateway-stub:9000
# DEVATTECH-131: Stripe test-mode secret key (starts with sk_test_...).
# Get one free at https://dashboard.stripe.com/test/apikeys -- no real
# card/business info required for test mode.
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# ── Bill Payments (Member 1) ────────────────────────────────────────────────
BILLER_API_URL=http://localhost:9000/mock-biller

# -- Security (Member 7) -------------------------------------------------
REQUIRE_ACTION_TOKEN=true

# -- Observability (Member 7) --------------------------------------------
LOG_FORMAT=text

# -- Limits/thresholds (merged from develop) ------------------------------
KYC_LIVENESS_THRESHOLD=0.7
DAILY_TRANSFER_LIMIT_USD=10000

# ── Exchange Forecast (DEVATTECH-94) -------------------------------------
FORECAST_MODEL=LightGBM

# ── Exchange Fee ----------------------------------------------------------
FEE_PCT=0.01

AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
S3_BUCKET=neobank-kyc
S3_REGION=us-east-1
S3_ENDPOINT_URL=  # leave blank for real AWS, set to http://localhost:9100 for local MinIO
